<%= form_with model: [post_image, post_comment], local: false  do |f| %>
  <%= f.text_area :comment, rows: '3', placeholder: "コメントをここに(100文字まで)", class: "form-control" %>
  <%= f.submit "送信", class: 'btn btn-md' %>
<% end %>