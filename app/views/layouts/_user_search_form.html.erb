<div class="search-form">
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %><br>
    <%= f.select :model, options_for_select({"ユーザー"=>"user", "投稿タイトル"=>"post_image", "猫の種類"=>"genre"}) %>
    <%= f.submit "検索" %>
  <% end %>
</div>