<h2>投稿一覧</h2>
  <% @post_images.each do |post_image| %>
  <div>
    <!--get_imageはmodelsに定義-->
    <%= image_tag post_image.get_image(300,300), class: "cat-photo" %>
    <p>タイトル:<%= post_image.title %></p>
    <p>説明:<%= post_image.caption %></p>
  </div>
  <% end %>