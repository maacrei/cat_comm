<div class="container">
  <h2><%= @post_image.title %></h2>
  <div class="row">
  <div class="col-8">

  <%= image_tag @post_image.get_image(800,800) %>
  </div>
  <div class="col-4">
    <h5>説明: <%= @post_image.caption %></h5>
    <!--投稿者が表示できない。アソシエーションおかしい？-->
    <h6>投稿者: <%= @post_image.customer.name %></h6>
    <h6>投稿日: <%= @post_image.created_at.strftime('%Y/%m/%d') %></h6>
  </div>
  </div>
</div>