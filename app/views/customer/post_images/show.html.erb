<div class="container">
  <h2><%= @post_image.title %></h2>
  <div class="row">
  <div class="col-8">

  <%= image_tag @post_image.get_image(800,800), class: "img-fluid" %>
  </div>
  <div class="col-4">
    <h5>説明<br><%= @post_image.caption %></h5>
    <!--投稿者が表示できない。customerで名前が保存できてない！後で確認-->
    <h6>投稿者: <%= @post_image.customer.name %></h6>
    <h6>投稿日: <%= @post_image.created_at.strftime('%Y/%m/%d') %></h6>
    <%= link_to "戻る", post_images_path %>
  </div>
  </div>
</div>